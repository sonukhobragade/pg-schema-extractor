[pytest]
# Without this, plain `pytest` cannot resolve the src.* imports; only
# `python -m pytest` works, because that puts the cwd on sys.path. The gate
# runs bare pytest, so the two disagreed.
pythonpath = .
testpaths = tests
